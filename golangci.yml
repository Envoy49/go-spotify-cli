run:
  deadline: 5m

linters:
  enable:
    - errcheck
    - gofmt
    - govet
    - golint
    - gocyclo
    - ineffassign
    - structcheck
    - varcheck
    - deadcode
    - unconvert

linters-settings:
  errcheck:
    check-type-assertions: true
    check-blank: true
